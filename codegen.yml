overwrite: true
schema: "./src/**/*.graphql"
documents: null
generates:
  ./src/types/generated.ts:
    plugins:
      - "typescript"
    config:
        skipTypename: true
        scalars:
          Int64: number
          DateTime: string
