type aws_igw @key(fields: "arn"){
  arn: String! @id
  id: String!
  owner: String
  attachments: [aws_igw_attachment]
  tags: [Tag]
  vpc: [aws_vpc] @hasInverse(field: igw)
}

type aws_igw_attachment {
  state: String
  vpcId: String @id
}