type aws_ebs @key(fields: "arn") {
  id: String!
  arn: String! @id
  iops: Int
  size: String
  state: String
  created: String
  snapshot: String
  encrypted: Boolean
  isBootDisk: Boolean
  volumeType: String
  availabilityZone: String
  multiAttachEnabled: Boolean
  tags: [Tag]
}
