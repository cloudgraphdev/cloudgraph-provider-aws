type awsIamOpenIdConnectProvider @key(fields: "arn") {
  id: String! @id @search(by: [hash])
  arn: String! @id @search(by: [hash, regexp])
  accountId: String! @search(by: [hash, regexp])
  serviceDisplayName: String @default(add: { value: "IAM Open ID Connect Provider"})
}
